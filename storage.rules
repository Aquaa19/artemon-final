rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to view product images for the shop
    // Restrict uploads/deletes to authenticated users (Admins)
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}